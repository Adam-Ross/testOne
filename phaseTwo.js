const thisIsPhaseTwo = true;
const featureOneA = (arr) => {
  arr.forEach((elem) => {
    console.log(elem);
  });
};

const arr = [1, 2, 3, 4];
const ouput = featureOneA(arr);
console.log(output);

function addedOneMoreFunctionInFeature() {
  return null;
}

const chagnedLocally = () => null;

function gonnaMakeItHappen() {
  return null;
}

// added changes to remote feature1A

// added from local

// There are many changes on the remote

// there are many changes locally on feature
